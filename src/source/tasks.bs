function StartTask(taskFunc as string, callback as string) as object
    task = createObject("roSGNode", `${taskFunc}_asynctask`)
    task.ObserveField("output", callback)
    task.control = "RUN"
    return task
end function
