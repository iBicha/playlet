' JobQueueTask should belong to JobQueue
' JobQueueTask starts with no JobTasks
' auto scales up and down
' JobTasks signal when they are ready to take on new work


function Init()
    m.top.functionName = "JobQueueTaskLoop"
end function

function JobQueueTaskLoop() as void
    msgPort = CreateObject("roMessagePort")

    m.top.observeField("shutdown", msgPort)
    m.top.observeField("request", msgPort)


end function
