namespace ArrayUtils

    function IndexOf(array as object, value as dynamic) as integer
        for i = 0 to array.Count() - 1
            if array[i] = value
                return i
            end if
        end for
        return -1
    end function

end namespace
